# mpvRex 4.6.6

- HTTP speed now uses actual per-second received bytes, including while paused, and immediately shows zero when traffic stops.
- Automatic subtitles inspect more title, URL, nested metadata, IMDb, season, episode, and year sources.
- P2P swarm counts now query both HTTP and UDP trackers; overlay order is buffered, seeds, peers, swarm, speed.
